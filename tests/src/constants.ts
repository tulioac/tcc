const PORT = 8080;
const URL = `http://localhost:${PORT}`;

const AUTHORS_QUANTITY = 3;

const TRACING_FILE_PATH = "tracing_results/tracing.json";

export { AUTHORS_QUANTITY, TRACING_FILE_PATH, URL };
